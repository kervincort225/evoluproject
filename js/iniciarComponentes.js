$(document).ready(function(){
	$('.scrollspy').scrollSpy(); // ->Para inicializar el scrollSpy
    $('.slider').slider(); // -> Inicia metodo para generar la libreria de imagenes del home (slider)
    $(".sidebar-collapse").sideNav(); //->Inicia metodo para generar el menu de amburgesa para moviles
    $('select').material_select();

});
